"""Retry Economist — failed-payment recovery research package.

Phase 1 ships only the synthetic world simulator (`retry_economist.generator`).
Nothing in this package makes network calls or depends on third-party libraries,
so a generated dataset is reproducible from a seed alone.
"""

__version__ = "0.1.0"
